{
  "version": "0.1.0",
  "name": "backend",
  "scripts": {
    "dev": "bun --env-file=./../.env run --hot ./index.ts",
    "build": "bun run ./index.ts"
  },
  "dependencies": {
    "hono": "^4.11.3",
    "mongoose": "^9.0.2"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}